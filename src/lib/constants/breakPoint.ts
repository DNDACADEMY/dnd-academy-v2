const small = '(max-width: 480px)';
const medium = '(max-width: 720px)';
const large = '(max-width: 960px)';
const xLarge = '(min-width: 1200px)';

const BREAK_POINT = {
  small,
  medium,
  large,
  xLarge,
};

export default BREAK_POINT;
