import { Meta, Typeset, Source } from "@storybook/blocks";

<Meta title="Token/Typography" />

export const { type, weight, size, letterSpacing, lineHeight, backgroundColor } = {
  type: {
    pretendard:
      "'Pretendard Variable', Pretendard, -apple-system, BlinkMacSystemFont, system-ui, Roboto, 'Helvetica Neue', 'Segoe UI', 'Apple SD Gothic Neo', 'Noto Sans KR', 'Malgun Gothic', sans-serif",
  },
  weight: {
    regular: "400",
    medium: "500",
    bold: "700",
    extrabold: "800",
  },
  size: {
    h1: 56,
    h2: 48,
    h3: 44,
    h4: 32,
    h5: 24,
    h6: 20,
    body1: 16,
    body2: 14,
    caption1: 12,
    caption2: 10,
  },
  letterSpacing: {
    h2: -0.48,
    h3: -0.44,
  },
  lineHeight: '140%',
  backgroundColor: "#13161C",
};

export const sampleText =
  "다람쥐 헌 쳇바퀴에 타고파 ‘Typography’, 1972. @ㅇ0ㅇ ₩0 〈♥〉 『♪』";

# Typography

**Font:** Pretendard

**FontSizes**

<Typeset
  fontSizes={[
    size.h1,
    size.h1,
    size.h2,
    size.h3,
    size.h4,
    size.h5,
    size.h6,
    size.body1,
    size.body2,
    size.caption1,
    size.caption2,
  ]}
  fontWeight={weight.bold}
  sampleText={sampleText}
  fontFamily={type.pretendard}
  
/>

**Weight:** 400(regular), 500(medium), 700(bold), 800(extrabold)

<Typeset
  fontSizes={[size.h6]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>
<Typeset
  fontSizes={[size.h6]}
  sampleText={sampleText}
  fontWeight={weight.medium}
  fontFamily={type.pretendard}
/>
<Typeset
  fontSizes={[size.h6]}
  sampleText={sampleText}
  fontWeight={weight.bold}
  fontFamily={type.pretendard}
/>
<Typeset
  fontSizes={[size.h6]}
  sampleText={sampleText}
  fontWeight={weight.extrabold}
  fontFamily={type.pretendard}
/>

## H1 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.h1}px;
line-height: ${lineHeight};
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.h1]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
  backgroundColor={backgroundColor}
/>

## H2 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.h2}px;
line-height: ${lineHeight};
letter-spacing: ${letterSpacing.h2}px;
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.h2]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>

## H3 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.h3}px;
line-height: ${lineHeight};
letter-spacing: ${letterSpacing.h3}px;
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.h3]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>

## H4 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.h4}px;
line-height: ${lineHeight};
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.h4]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>

## H5 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.h5}px;
line-height: ${lineHeight};
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.h5]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>

## H6 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.h6}px;
line-height: ${lineHeight};
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.h6]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>

## body1 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.body1}px;
line-height: ${lineHeight};
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.body1]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>

## body2 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.body2}px;
line-height: ${lineHeight};
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.body2]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>

## caption1 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.caption1}px;
line-height: ${lineHeight};
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.caption1]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>

## caption2 Font

**Styles:**

<Source
  language="css"
  code={`
font-weight: normal; // normal, 500, bold, 800
font-size: ${size.caption2}px;
line-height: ${lineHeight};
`}
  format
  dark
/>

<Typeset
  fontSizes={[size.caption2]}
  sampleText={sampleText}
  fontWeight={weight.regular}
  fontFamily={type.pretendard}
/>
